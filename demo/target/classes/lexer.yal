{
    import com.duarte.lexer.returnToken;
}

let digit = ['0'-'9']
let number = digit+
let blanks = ' '
let sum = '+'

rule gettoken =
blanks { return lexbuf }
| digit { return DIGIT }
| number { return NUM }
| sum { return SUM }